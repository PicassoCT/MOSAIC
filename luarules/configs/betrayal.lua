-- All times are simulation frames (30 fps), distances are elmos.
return {
    updateFrames = 15,
    selfDestructFrames = 150,
    targetRunSeconds = 35,
    minRunDistance = 900,
    maxRunDistance = 2400,
    escapeDistance = 600,
    deliveryRadius = 100,
    deliveryFrames = 60,
    dropLifetimeFrames = 5400,
    stalledFrames = 900,
    revealFrames = 9000,
}
