-- All times are simulation frames (30 fps), distances are elmos.
return {
    updateFrames = 15,
    selfDestructFrames = 150,
    targetRunSeconds = 35,
    minRunDistance = 900,
    maxRunDistance = 2400,
    minEscapeDistance = 600,
    escapeSearchRadius = 1800,
    escapeContactClearance = 900,
    escapeUnitClearance = 300,
    escapeWeaponBuffer = 150,
    escapeExitMargin = 48,
    deliveryRadius = 100,
    deliveryFrames = 60,
    dropLifetimeFrames = 5400,
    stalledFrames = 900,
    revealFrames = 9000,
}
